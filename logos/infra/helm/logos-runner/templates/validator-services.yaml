{{- $root := . -}}
{{- $nodes := default (list) .Values.nodes.nodes }}
{{- range $i, $node := $nodes }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "logos-runner.fullname" $root }}-node-{{ $i }}
  labels:
    {{- include "logos-runner.nodeLabels" (dict "root" $root "index" $i) | nindent 4 }}
spec:
  type: NodePort
  selector:
    {{- include "logos-runner.nodeLabels" (dict "root" $root "index" $i) | nindent 4 }}
  ports:
    - name: http
      port: {{ default 18080 $node.apiPort }}
      targetPort: http
    - name: testing-http
      port: {{ default 18081 $node.testingHttpPort }}
      targetPort: testing-http
    - name: swarm-udp
      port: {{ default 60000 $node.networkPort }}
      targetPort: swarm-udp
      protocol: UDP
{{- end }}
